# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (single `README.md`). There is no application code, no build system, no dependencies, and no services to run. Future agents should check for newly added `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or similar dependency manifests and adjust their workflow accordingly once code is added.
