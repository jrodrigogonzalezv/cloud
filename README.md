# React Cloud App

App React creada con Vite y TypeScript.

## Scripts

- `npm run dev`: inicia el servidor de desarrollo.
- `npm run build`: compila TypeScript y genera la version de produccion.
- `npm run preview`: sirve localmente la build generada.